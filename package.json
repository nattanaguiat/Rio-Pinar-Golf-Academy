{
  "name": "rio-pinar-golf-academy",
  "version": "1.0.0",
  "description": "Full MERN + Admin app for Rio Pinar Golf Academy",
  "main": "backend/server.js",
  "type": "module",
  "scripts": {
    "start": "cd backend && npm start",
    "start:dev": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd backend && npm i && cd ../frontend && npm i && ../admin && npm i",
    "build": "cd client && npm run build",
    "clean": "rm -rf node_modules client/node_modules client/dist server/node_modules",
    "seed": "cd server && npm run seed"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
